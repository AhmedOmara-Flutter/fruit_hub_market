import '../../../../core/utils/app_imports.dart';

class OrderTrackingPageView extends StatelessWidget {
  const OrderTrackingPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Order Tracking Page'),
    );
  }
}
